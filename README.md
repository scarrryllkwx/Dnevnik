# Локальный дневник

Откройте `login.html` в браузере.

## Аккаунты и пароли

Почты и пароли вынесены в отдельный файл `accounts.js`, который добавлен в `.gitignore` и **не попадает в репозиторий**. В коде (`app.js`) логинов и паролей нет.

Пароли хранятся **не в открытом виде**, а как SHA-256-хеш (поле `passwordHash`). При входе введённый пароль хешируется в браузере и сравнивается с сохранённым хешем.

### Первый запуск

Если файла `accounts.js` нет, скопируйте `accounts.example.js` в `accounts.js` и впишите свои данные.

### Как добавить или изменить аккаунт

1. Откройте `hash-tool.html` в браузере.
2. Введите нужный пароль — получите его SHA-256-хеш (он же скопируется в буфер обмена).
3. В `accounts.js` добавьте/измените запись:

   ```js
   const ACCOUNTS = [
     { name: "Имя", email: "you@example.com", passwordHash: "<вставленный хеш>" },
   ];
   ```

4. При необходимости поменяйте `DEFAULT_TEACHER_EMAIL` — это email учителя по умолчанию.

## Хранение данных

Записи дневника сохраняются локально в браузере через `localStorage`.

## О безопасности

Это приложение полностью клиентское (без сервера), поэтому `accounts.js` всё равно загружается в браузер и доступен любому, у кого есть файлы приложения. Вынос в отдельный gitignore-файл и хеширование паролей защищают от утечки через репозиторий и от хранения паролей в открытом виде, но не заменяют настоящую серверную авторизацию.
